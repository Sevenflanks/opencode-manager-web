# Domain docs

本專案採單一領域文件結構：`CONTEXT.md` 領域詞彙表、`docs/adr/` 已確認且值得保留取捨理由的架構決策。

探索、設計與撰寫工作票前，先讀 `CONTEXT.md`，以及與任務相關的 ADR。使用詞彙表中的名稱；`CONTEXT.md` 不放實作規格或待辦事項。若提案與既有 ADR 衝突，明確指出衝突與重新討論的理由。

ADR 目錄或文件尚不存在時，直接繼續工作；有實際決策需要記錄時才建立。
